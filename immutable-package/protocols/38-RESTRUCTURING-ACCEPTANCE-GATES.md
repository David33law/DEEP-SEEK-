# 38 — RESTRUCTURING ACCEPTANCE GATES (v1.2) — all mechanical, all mandatory
G1 Capability preservation: every previously certified capability green on preserved regression set.
G2 New capabilities: prove themselves on legal TRANSFER cases (10), trace-graded.
G3 Provenance totality: every output of the new structure carries provenance + reasoning trace (ledger audit probe).
G4 Rollback/replay: checkpoint-restore and event-replay verified ON THE NEW TREE (VD-15/16 class).
G5 Self-model truth: capability/component registry reflects the ACTUAL new structure (diff against filesystem/system defs = empty).
G6 Entry points: production entry points resolve to the new certified path (reachability scan proof).
G7 Retirement reality: retired components unreachable (import/require/system-def scan = zero references).
G8 API independence: deterministic + certified capabilities run with ALL external AI adapters disabled (network-off run).
G9 Certification report: before/after comparison (capabilities, scores, costs, risks) published with hashes.
RESTRUCTURING_CERTIFIED requires G1-G9 PASS + independent audit (22) extension over the migration ledger.
